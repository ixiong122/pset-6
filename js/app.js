let emptyArray = [];
window.onload = function() {
  document.getElementById("events")
  let h =  document.getElementById("events")
}
letters.push(h);

get elements, get input, store it into the array, take the array
